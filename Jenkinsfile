pipeline {
        agent {
            label 'docker-gce'
        }
    stages {
        stage('Build') { 
            steps {
                sh 'echo ls '
                sh 'pwd'
                sh 'ls'
            }
        }
    }
}
